import react from 'react';
// import users from '../../../profile/src/users.json';
import Follower from './Follower.js'

export default function Followers(){
    // const user = users[0];


    return(
        <section id="aside">
            {/* {user.following.map(follow => <Follower follower={users[follow]}/>)} */}
        </section>
    );
}