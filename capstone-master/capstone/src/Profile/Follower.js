import react from 'react'
// import users from '../../../profile/src/users.json';

export default function Follower(follower){

    console.log(follower);

    return (
        <div>
            {/* <img src={follower.follower.media_link}/>
            <p>{follower.follower.username}</p> */}
        </div>
    );
}